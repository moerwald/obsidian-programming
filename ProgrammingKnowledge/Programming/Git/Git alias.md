
Aliase können via `git config --global alias.ALIASNAME cmd` gesetzt werden.

Beispiel für pretty Log:

```
git config --global alias.lg "log --pretty='%C(red)%h%Creset%C(yellow)%d%Creset %s %C(cyan)(%ar)%Creset'"
```


#Git