MsBuild erlaubt via `-bl` Switch ein binäres Build Log zu erstellen. Das Log kann mit Hilfe des [MsBuildStructuredLogViewer](https://msbuildlog.com/) analysiert werden.

```
dotnet build -bl .\RecorderApp.csproj
.\msbuild.binlog
```

#MSBuild 
#dotnetbuild 