Logging kann im File `postgresql.conf` aktiviert werden.

Einfach `log_destination` , `log_filename` , `log_rotation_age` und `log_rotation_size` aktivieren.

Danach post-gresql -Service restarten

#Logging 
#Postgre 